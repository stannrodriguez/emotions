{
  "name": "emotion-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Emotion Atlas (working name) — a mobile-first, on-device emotion wheel. No backend, no build step.",
  "scripts": {
    "verify": "node scripts/verify-data.mjs",
    "start": "python3 -m http.server 4173",
    "verify:ui": "node scripts/verify-ui.mjs",
    "verify:all": "npm run verify && npm run verify:ui"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
